import 'package:flutter/widgets.dart';

class ChallengesViewBody extends StatelessWidget {
  const ChallengesViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Challenges'));
  }
}
